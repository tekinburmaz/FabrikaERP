<UserControl x:Class="FabrikaERP.Views.Pages.HrPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FabrikaERP.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- KPI ŞERİDİ -->
        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,20">
            <Border Background="#161B22" CornerRadius="12" BorderBrush="#30363D" BorderThickness="1" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Toplam Personel" Foreground="#8B949E" FontSize="12"/>
                    <TextBlock Text="{Binding TotalEmployeeCount}" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,5"/>
                </StackPanel>
            </Border>
            <Border Background="#161B22" CornerRadius="12" BorderBrush="#30363D" BorderThickness="1" Padding="15" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="İzinli Personel" Foreground="#8B949E" FontSize="12"/>
                    <TextBlock Text="{Binding OnLeaveCount}" Foreground="#E3B341" FontSize="24" FontWeight="Bold" Margin="0,5"/>
                </StackPanel>
            </Border>
            <Border Background="#161B22" CornerRadius="12" BorderBrush="#30363D" BorderThickness="1" Padding="15" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Bekleyen İzinler" Foreground="#8B949E" FontSize="12"/>
                    <TextBlock Text="{Binding PendingLeaves.Count}" Foreground="#58A6FF" FontSize="24" FontWeight="Bold" Margin="0,5"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Personel Tablosu -->
            <Border Grid.Column="0" Background="#161B22" CornerRadius="12" BorderBrush="#30363D" BorderThickness="1" Margin="0,0,15,0">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Padding="15,12" BorderBrush="#30363D" BorderThickness="0,0,0,1">
                        <TextBlock Text="Personel Listesi" Foreground="White" FontWeight="Bold"/>
                    </Border>
                    <DataGrid ItemsSource="{Binding Employees}" AutoGenerateColumns="False" 
                              Background="Transparent" BorderThickness="0" Foreground="White" 
                              RowBackground="#161B22" AlternatingRowBackground="#0D1117">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Kod" Binding="{Binding EmployeeCode}" Width="80"/>
                            <DataGridTextColumn Header="Ad Soyad" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="Departman" Binding="{Binding Department}" Width="*"/>
                            <DataGridTextColumn Header="Ünvan" Binding="{Binding JobTitle}" Width="*"/>
                            <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- İzin Onayları -->
            <Border Grid.Column="1" Background="#161B22" CornerRadius="12" BorderBrush="#30363D" BorderThickness="1">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Padding="15,12" BorderBrush="#30363D" BorderThickness="0,0,0,1">
                        <TextBlock Text="Bekleyen İzin Onayları" Foreground="White" FontWeight="Bold"/>
                    </Border>
                    <ItemsControl ItemsSource="{Binding PendingLeaves}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12" BorderBrush="#30363D" BorderThickness="0,0,0,1">
                                    <Grid>
                                        <StackPanel>
                                            <TextBlock Text="{Binding EmployeeId}" Foreground="White" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding StartDate, StringFormat=d}" Foreground="#8B949E" FontSize="11"/>
                                            <TextBlock Text="{Binding Reason}" Foreground="#C9D1D9" FontSize="11" TextWrapping="Wrap" Margin="0,5"/>
                                        </StackPanel>
                                        <Button Content="Onayla" HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                Command="{Binding DataContext.ApproveLeaveCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding Id}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
